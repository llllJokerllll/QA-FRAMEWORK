[build]
builder = "DOCKERFILE"
dockerfilePath = "dashboard/frontend/Dockerfile.prod"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "qa-framework-frontend"
